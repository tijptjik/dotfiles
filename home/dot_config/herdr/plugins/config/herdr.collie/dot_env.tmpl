# Collie is a tailnet-only remote terminal control surface.
# The tailnet suffix is stable for this personal Tailscale account; the host
# portion follows the machine name, so every agent host validates its own URL.
COLLIE_TRUSTED_USER=m@type.hk
COLLIE_PUBLIC_HOSTS={{ .chezmoi.hostname }}.tailb49776.ts.net
COLLIE_MULTI_SESSION=1
COLLIE_PORT=18787
