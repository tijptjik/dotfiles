# ~/.config/hypr/hyprlock.conf
source = ~/.config/hypr/colors.conf

general {
    hide_cursor = true
    ignore_empty_input = true
    immediate_render = false
    text_trim = true
    fail_timeout = 1000
}

{{- if .isLaptop }}
# The laptop panel is 3840x2400 at scale 2 (1920x1200 logical).  Keep all
# primary widgets within that 1200px logical height.
background {
    path = screenshot
    blur_passes = 3
    blur_size = 5
    brightness = 0.5
    color = $fx-magenta
}

label {
    text = cmd[update:1000] echo "$(date +"%-H:%M")"
    color = $fx-white
    font_size = 510
    font_family = ETBembo
    position = 0, 230
    halign = center
    valign = center
}

label {
    text = <span line_height="1.22" allow_breaks="true" font_weight="light">Everything transitory is but an image.</span>
    color = $fx-magenta
    font_size = 78
    font_family = ETBembo
    position = 0, -260
    halign = center
    valign = center
}

input-field {
    size = 630, 96
    outline_thickness = 0
    dots_size = 0.24
    dots_spacing = 0.5
    dots_center = true
    font_color = $fx-white
    outer_color = rgba(fef1fd33)
    inner_color = rgba(32303e00)
    # Keep the input visually continuous while authentication is checked or fails.
    check_color = rgb(555169)
    fail_color = rgb(555169)
    fade_on_empty = false
    font_family = ETBembo
    fail_text = <i></i>
    placeholder_text = <span allow_breaks="true" font_weight="bold" foreground="##fef1fdaa">●   ●   ●</span>
    hide_input = false
    # Lift the field slightly from its lower placement.
    position = 0, -600
    halign = center
    valign = center
}
{{- else if .isDesktop }}
background {
    monitor = DP-1
    path = screenshot
    blur_passes = 3
    blur_size = 5
    brightness = 0.5
    color = $fx-magenta
}

background {
    monitor = DP-2
    path = screenshot
    blur_passes = 3
    blur_size = 5
    brightness = 0.5
    color = $fx-magenta
}

label {
    monitor = DP-1
    text = cmd[update:1000] echo "$(date +"%-H:%M")"
    color = $fx-white
    font_size = 400
    font_family = Josefin Sans
    position = -720, -420
    halign = center
    valign = top
}

label {
    monitor = DP-1
    text = <span line_height="1.4" style="italic" allow_breaks="true" font_weight="light">Whatever you dream you can,<br/>begin it. Boldness has genius,<br/>power and magic in it!</span>
    color = $fx-magenta
    font_size = 56
    font_family = DiariaPro-LightItalic
    position = 640, 70
    halign = center
    valign = center
}

label {
    monitor = DP-2
    text = <span line_height="1.2" style="italic" allow_breaks="true" font_weight="light">Unto the world<br/>in grossly loving zest</span>
    color = $fx-magenta
    font_size = 72
    font_family = DiariaPro-LightItalic
    position = 0, 32
    halign = center
    valign = center
}

input-field {
    monitor = DP-1
    size = 500, 80
    outline_thickness = 0
    dots_size = 0.24
    dots_spacing = 0.5
    dots_center = true
    font_color = $fx-white
    outer_color = rgba(fef1fd33)
    inner_color = rgba(32303e00)
    check_color = rgb(555169)
    fail_color = rgb(555169)
    fade_on_empty = false
    font_family = tijpset
    placeholder_text = <span allow_breaks="true" font_weight="bold" foreground="##fef1fdaa">●   ●   ●</span>
    hide_input = false
    position = 0, -320
    halign = center
    valign = center
}
{{- end }}
