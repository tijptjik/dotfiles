#!/usr/bin/env fish

# Wrapper function to open markdown files in glow, else fallback to bat

set file $argv[1]

if string match -q "*.md" -- $file
or string match -q "*.markdown" -- $file
    glow -p "$file"
else
    bat "$file"
end
